# 对照笔记：mini-agent（Unit 2 对照组）

> 决策表的左边一列。这页不是给它写简介——是把它当**基准零点**：其它四页的
> 「替你付掉了什么」都在跟这页的行数对账（tablegen 对照行：四个被替掉的零件
> 共 86 行，solution 口径）。写完这页，「已拥有」才不是一句口号。
> 固定小节五个（`tests/test_notes_meta.py` 按结构把关）；每节下面的问题是脚手架，
> 回答完删掉问题、留下结论——`TODO(笔记)` 占位符必须全部替换。

## 它替 mini-agent 付掉了什么

对照组没有「它」——所以这节要回答的是反问题：**这 86 行里每一行在买什么**
（`agent.py::ReActAgent.run` 的循环与双终止、`tools.py` 的 schema 生成与分发、
`structured.py::ask_structured` 的校验回喂）。翻 unit2-mini-agent/milestone/
的三个 TODO 文件，按「零件 → 它给的控制权」写三五行。

- TODO(笔记)：按零件列清单，每行注明「这行买到的控制权是什么」（例：回喂格式想改就改）。

## 它没替你付什么

反方向也要诚实：mini-agent 没替你付的东西比四个框架都多。至少盘点五件
（对照 L3.6 对照表的右列）：流式可观测、检查点/暂停恢复（L3.3 才有）、
多 agent 转交、会话存储、评估工具链——写你自己踩过的「想加但没有」。

- TODO(笔记)：列「没替你付」清单，每条注一句「补上它大概要几行/要什么件」。

## 最惊讶的一个机制

候选（挑一个，注明课次）：协议级 mock 端点让 agent 也能零 key 做契约测试
（L2.3/L3.x 全学段）；或 CALL_LOG 证明「工具真被执行」而非模型自说自话
（test_contract 的取证设计）。为什么它惊讶：Java 里等价物是什么（WireMock？）。

- TODO(笔记)：一个机制 + 课次 + 一句「为什么惊讶」+ Java 对照物。

## 锁定性一句话

零框架依赖——代价是每个行为都是你要亲手的行，收益是每个行为都是你能改的行。
用你自己的话写一句（这句会被 summary.py 抓进决策表定稿）。

- TODO(笔记)：一句话，别超一行。

## 什么时候选它

至少给两个具体场景（提示方向：教学/原型；极端合规要求「每个行为可审计可改」；
依赖面必须极小时）。再写一个反例场景（什么时候它明显不划算）。

- TODO(笔记)：两个选它的场景 + 一个不选它的场景，各一句理由。
