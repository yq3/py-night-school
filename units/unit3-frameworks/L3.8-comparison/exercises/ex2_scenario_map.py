# 练习 2（单变量编辑约束：只改本文件 TODO 标注的函数体/区域**与所需的顶部 import**，其余不要动）
"""场景到选型映射：把六个真实选型场景放进决策表（本课 Step4 的操作性检验）。

六个场景各有一个「最优起点」（讲义 §3 Step4 的「何时选谁」）——不是唯一解，
但每个场景都有一格的显著优势压倒其他（HITL 机制 / 零锁定 / harness 三件套 /
调试器+eval / 平台画布 / 极简原语）。你的任务：

  1. CHOICES：每个场景 id 映射到 FRAMEWORKS 之一；
  2. REASONS：每个场景一段理由——必须非空、不少于 15 字、且**带课次证据**
     （出现 L3.1–L3.7 / Unit 2 / milestone / mini-agent 之一——理由要能回指数据页）。

完成判据（exercises/test_ex2.py，3 个测试）：映射与验收持有的期望表逐格一致；
六个场景一个不漏；六条理由全部非空、够长且带课次证据。

自查法（讲义 §3 Step4 讲过）：先盖住讲义自己填，再对照——两处不一致时，
先怀疑自己回忆的决策表，再怀疑题目。
"""

from __future__ import annotations

SCENARIOS: dict[str, str] = {
    "s1": "金融合规场景：每单必须断电可恢复（暂停→杀进程→审批→新进程续跑），状态要落在自己持有的存储里。",
    "s2": "两周即弃的一次性 POC：把两个 mock 工具串成一次审查，交付后整个项目直接扔掉。",
    "s3": "多代理研究原型：主代理派活给专职子代理、要虚拟文件台写底稿、还要跨会话记忆。",
    "s4": "团队已在 Google 栈（GCP / Vertex）：要浏览器调试器看事件流，要内建 eval 工具链做回归。",
    "s5": "流程由业务同学自己在画布上改（不写代码），跑到人工节点要弹表单、按按钮分流。",
    "s6": "想要最薄的原语层：双 agent 转交（换 agent 不换对话）就够，未来可能深度定制循环内脏。",
}

FRAMEWORKS = ("mini-agent", "openai-agents", "langgraph", "deepagents", "adk", "dify")

# TODO(ex2): 把每个场景的 id 映射到 FRAMEWORKS 之一（六个键都要填）
CHOICES: dict[str, str] = {
    "s1": "",
    "s2": "",
    "s3": "",
    "s4": "",
    "s5": "",
    "s6": "",
}

# TODO(ex2): 每个场景一段选型理由：≥15 字且带课次证据（L3.x / Unit 2 / milestone）
REASONS: dict[str, str] = {
    "s1": "",
    "s2": "",
    "s3": "",
    "s4": "",
    "s5": "",
    "s6": "",
}
