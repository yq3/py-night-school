# 参考答案

先完成练习再进来。对照要点：不追求与你的写法一致，追求**通过验收 + 读得舒服**。
ex2 的用例表展示了三类值得写的用例：**边界**（恰好等于上限应 PASS）、**冲突**（脏数据/单笔/合计同时违规时，规则顺序决定返回谁）、**覆盖**（四种结果各就各位——meta-test 机器判定的正是这一点）。
ex3 的知识点是「格式化器的能力边界」：ruff format 永远不会改写字符串内容，长字符串与注释的拆行只能靠人。
