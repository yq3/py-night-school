"""expense 包：报销单预审规则（rules）与命令行入口（cli）。

这个文件本身几乎是空的——它只干一件事：告诉 import 系统「这个目录是一个包」。
（Python 3.3+ 即使没有它，目录也能当「命名空间包」用；但工程惯例是显式写出来：
包身份一目了然，也避开命名空间包在多目录搜索时的歧义。）

对照 Java：包由目录结构 + package 声明共同决定；Python 的包 = 目录 + 这个文件，
文件名 __init__.py 是固定约定的「双下划线名字」（dunder）之一——同类还有
__name__、__pycache__，本课 §2 会专门讲这个命名体系。
"""
